{
  "name": "uspa",
  "private": true,
  "version": "1.0.0",
  "packageManager": "npm@11.13.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "turbo": "^2.10.5",
    "prettier": "^3.6.2"
  }
}